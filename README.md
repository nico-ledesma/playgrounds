# Playgrounds

A collection of small Node.js playgrounds for experimenting with libraries and patterns.

## Usage

1. Install dependencies: `npm install`
2. Start playgrounds: `node index.js` or `npm run start`.
